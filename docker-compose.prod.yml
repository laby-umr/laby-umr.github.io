# 供 GitHub Actions 部署到服务器时使用（镜像名由 workflow 替换为 ghcr.io/用户名/仓库名:latest）
services:
  blog:
    image: IMAGE_PLACEHOLDER
    container_name: laby-blog
    ports:
      - "8089:80"
    restart: unless-stopped
